import { makeStyles } from '@material-ui/styles'

export default makeStyles((theme) => ({
  container: {
    width: "100%"
  },   
  textInput: {
    width: "100%",
    marginBottom: '12px',
  },
  input: {    
    height: "36px",
  },
  submitBtn: {
    marginTop: "12px",
    width: "100%",
    backgroundColor: "#00a000"
  }
}))
